arr = [[1,2],[2,3],[4,5]]
len(arr)     -> 3
len(arr[0])  -> 2 


정수(x)자리수 끊는법
list(str(x))   -> 정수를 리스트로
list로 각 배열로 핸들링
int(x[i])    -> 리스트를 정수로
int(''.join(x)) -> 리스트를 정수로

''.join(x)   -> 리스트를 문자열

arr.remove(min(arr))


"try hello world"
s_split = s.split(" ")
->"try hello world"

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
list.sort(reverse=True). -> 내림차순 정렬
list(map(int, revered(str(s)))) -> 역순

첫번째는 [정렬]이지만, 두번째는 [뒤집어] 출력 이다.
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ